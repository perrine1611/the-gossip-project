
<h1> Création d'un potin </h1>

  <body>

    <h2> Pour créer un gossip, remplir ce formulaire : </h2>
    <%= form_tag url_for(action: 'create'), method: "post" do %>

      <% if @gossip.errors.any? %>
      <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
      <ul>
        <% @gossip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    <% end %>

    <%= label_tag 'Qui es-tu ?' %>
    <%= text_field_tag 'auteur', @gossip.user %>
    <br /> <br />

    <%= label_tag 'Titre du gossip :' %>
    <%= text_field_tag 'title', @gossip.title %>
    <br /> <br />

    <%= label_tag 'Quel est le potin ?' %>
    <%= text_area_tag 'content', @gossip.content %>
    <br /> <br />
    
    <%= submit_tag "Envoyer" %>

  <% end %>

  </body>
